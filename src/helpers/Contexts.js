// File that contains all of our Contexts that we want to have in our application. Teacher recommends adding things like this to a "helper" folder //

import {createContext} from 'react';

// Pass an empty string as the initial game context value
export const GameStateContext = createContext("");